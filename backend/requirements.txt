# FreeGraduates AI Resume Builder - Backend Dependencies
# Python 3.12+ required

# --- Web framework (replaces Flask with FastAPI for async, OpenAPI, modern) ---
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
gunicorn>=22.0.0
python-multipart>=0.0.9

# --- Configuration ---
python-dotenv>=1.0.0
pydantic>=2.6.0
pydantic-settings>=2.2.0

# --- Auth (Firebase ID Token verification) ---
firebase-admin>=6.4.0

# --- AI / LangGraph workflow ---
# Google Gemini is the active LLM provider.
google-genai>=1.0.0
langgraph>=0.0.1
langchain-core>=0.1.0

# --- PDF processing (LinkedIn / resume upload) ---
pdf2image>=1.17.0
Pillow>=10.2.0
pypdf>=4.0.0

# --- DOCX export ---
python-docx>=1.1.0

# --- PDF export ---
weasyprint>=62.0

# --- Markdown ---
markdown>=3.6