{
  "name": "freegraduates-backend",
  "version": "2.0.0",
  "description": "FreeGraduates AI Resume Builder FastAPI Backend",
  "main": "main.py",
  "scripts": {
    "postinstall": "pip install -r requirements.txt || python3 -m pip install -r requirements.txt || true",
    "build": "pip install -r requirements.txt || python3 -m pip install -r requirements.txt",
    "start": "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
  },
  "keywords": [
    "fastapi",
    "resume-builder",
    "langgraph",
    "gemini"
  ],
  "license": "CC-BY-NC-4.0"
}
